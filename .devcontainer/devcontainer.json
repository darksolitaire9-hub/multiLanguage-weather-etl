#!/bin/bash
set -e  # Exit on error
set -x  # Print commands (helpful for debugging)

echo "================================================"
echo "🚀 Starting Minimal Devcontainer Setup"
echo "================================================"

# Verify base environment
echo "📦 Verifying Python Bullseye base..."
python --version
which python

echo "📦 Checking apt package manager..."
sudo apt-get update

echo "📦 Installing essential build tools..."
sudo apt-get install -y --no-install-recommends \
    build-essential \
    curl \
    wget \
    ca-certificates

echo "================================================"
echo "✅ Base setup complete!"
echo "================================================"
